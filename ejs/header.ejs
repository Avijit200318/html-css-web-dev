<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link href="https://cdn.jsdelivr.net/npm/remixicon@4.0.0/fonts/remixicon.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
  <% if(nav===true){ %>
    <nav class="w-full px-10 py-3 flex  justify-between items-center border-b-4 border-bg-zinc-300">
      <div class="logo flex">
        <div class="w-7 h-7 rounded-full overflow-hidden">
          <img class="w-full h-full object-cover" src="/images/logo.png" alt="">
        </div>
        <h3 class="text-xl font-semibold ">Pinterest</h3>
      </div>
      <form id="searchForm" action="/searchText" method="post">
        <input id="searchInput" class="w-[60vw] bg-gray-300 py-2.5 px-4 rounded-full" type="text" placeholder="Search" required name="searchTxt" autocomplete="off">
      </form>
      <div class="flex gap-10 items-center">
        <a id="profile" class="text-lg py-1.5 px-3 rounded-full transition-all duration-500 hover:bg-black hover:text-white <%= activePage === 'profile'? 'active':'' %>" href="/profile">Profile</a> 
        <a id="feed" class="text-lg py-1.5 px-3 rounded-full transition-all duration-500 hover:bg-black hover:text-white <%= activePage === 'feed'? 'active':'' %>" href="/feed">Feed</a>
        <a class="px-3 py-2 bg-red-600 text-white rounded-lg transition-all duration-500 hover:bg-red-700" href="/logout">Logout</a>
      </div>
    </nav>
    <script>
      document.getElementById('searchInput').addEventListener('keydown', function(event) {
        // Check if the pressed key is Enter (key code 13)
        if (event.keyCode === 13) {
          // Prevent the default form submission behavior
          event.preventDefault();
          // Trigger the form submission
          document.getElementById('searchForm').submit();
        }
      });
      </script>
    <% } %>